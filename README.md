# Asphalt Tonnage Calculator

This is a simple website built to calculate the tonnage of Asphalt

## What It Does

The website estimates how many tons of asphalt are needed for a paving project.

The user enters:

- Length in feet
- Width in feet
- Thickness in inches

The calculator then gives an estimated asphalt tonnage.
The calculator will give an error message if negative numbers are inputed.

## Formula

tons = length × width × thickness(ft) × 145 ÷ 2000

The calculator uses 145 pounds per cubic foot as an estimated asphalt density.

## What I Used

This project uses one `index.html` file with:

- HTML for the page structure
- CSS for simple styling
- JavaScript for the calculator
